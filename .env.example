# Chave da Google Places API para puxar as avaliações reais do Google no build.
# Copie para .env (que fica fora do git) e rode: npm run reviews
GOOGLE_PLACES_API_KEY=

# Opcional: pula a busca por texto e vai direto no place.
# GOOGLE_PLACE_ID=
